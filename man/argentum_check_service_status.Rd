% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_service_status.R
\name{argentum_check_service_status}
\alias{argentum_check_service_status}
\title{Check WFS Service Status}
\usage{
argentum_check_service_status(organization = NULL, interactive = FALSE)
}
\arguments{
\item{organization}{Optional. Name of a specific organization to check. If NULL, checks all organizations or prompts for selection.}

\item{interactive}{Logical. If TRUE, prompts the user to select organizations interactively.}
}
\value{
A list containing the results data frame and summary statistics.
}
\description{
This function checks the status of WFS services for all organizations, a specific organization,
or interactively selected organizations. It attempts to read layers from each service and
returns "Available" if successful, or "Unavailable" if not. The results are displayed in an
interactive table with summary statistics.
}
\examples{
# Check all organizations
result <- argentum_check_service_status()

# Check a specific organization
result <- argentum_check_service_status("Example Organization")

# Interactive selection (run only in interactive sessions)
if(interactive()) {
  result <- argentum_check_service_status(interactive = TRUE)
}

# View the interactive table
if(interactive()) {
  result$table
}
}
